{
  "name": "ui-core",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --ignore-path ../../.prettierignore --check . && eslint --ignore-path ../../.eslintignore .",
    "format": "prettier --ignore-path ../../.prettierignore --write ."
  },
  "dependencies": {
    "class-variance-authority": "^0.6.1",
    "clsx": "^1.2.1",
    "tailwind-merge": "^1.13.2"
  },
  "devDependencies": {
    "eslint": "^8.44.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "^2.8.8",
    "tailwind-config": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^5.1.6"
  }
}
